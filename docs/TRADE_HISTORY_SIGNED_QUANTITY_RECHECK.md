# Trade History SignedQuantity 重新检查报告

## 检查时间
2025-11-13（清理数据库后重新同步）

## 1. 数据概览

**总记录数**: 200 条

### 1.1 总体统计

| 指标 | 数量 | 占比 |
|------|------|------|
| 总记录数 | 200 | 100% |
| 有 `signed_quantity` 的记录 | 200 | 100% |
| `signed_quantity > 0` (正数) | **200** | **100%** |
| `signed_quantity < 0` (负数) | **0** | **0%** |
| `signed_quantity = 0` | 0 | 0% |
| `signed_quantity IS NULL` | 0 | 0% |

**关键发现**：✅ **所有记录的 `signed_quantity` 都是正数**（100%）

### 1.2 按 Action 和 Side 分组统计

| Action | Side | 总记录数 | 正数 | 负数 | 正数占比 |
|--------|------|---------|------|------|---------|
| `open_long` | `long` | 30 | 30 | 0 | 100% |
| `close_long` | `long` | 10 | 10 | 0 | 100% |
| `open_short` | `short` | 67 | **67** | **0** | **100%** |
| `close_short` | `short` | 93 | **93** | **0** | **100%** |

**关键发现**：
- ✅ Long 方向：100% 为正数（符合预期）
- ✅ **Short 方向：100% 为正数**（新 API 行为）

### 1.3 按 `start_position` 状态分组（Short 方向）

| `start_position` 状态 | 总记录数 | 正数 | 负数 | 占比 |
|----------------------|---------|------|------|------|
| **NEGATIVE** | 115 | 115 | 0 | 72% |
| **ZERO** | 45 | 45 | 0 | 28% |
| **NULL** | 0 | 0 | 0 | 0% |

**关键发现**：
- ✅ **所有 Short 方向记录都有 `start_position` 字段**（无 NULL）
- ✅ 当 `start_position` 为负数时，`signed_quantity` 为正数（115 条）
- ✅ 当 `start_position` 为 0 时，`signed_quantity` 也为正数（45 条）

### 1.4 API 版本特征

| 特征 | 数量 | 占比 |
|------|------|------|
| 有 `start_position` 字段 | 200 | 100% |
| 有 `exchange_side` 字段 | 200 | 100% |
| `start_position` 为 NULL | 0 | 0% |

**结论**：✅ **所有记录都使用新版本 API 格式**

## 2. 与之前数据的对比

### 2.1 之前的数据（清理前）

| 指标 | 数量 | 占比 |
|------|------|------|
| 总记录数 | 449 | 100% |
| Short 方向正数 | 93 | 20.7% |
| Short 方向负数 | 160 | 35.6% |
| 有 `start_position` | 249 | 55.5% |
| 无 `start_position` | 200 | 44.5% |

### 2.2 新同步的数据（清理后）

| 指标 | 数量 | 占比 |
|------|------|------|
| 总记录数 | 200 | 100% |
| Short 方向正数 | **160** | **100%** |
| Short 方向负数 | **0** | **0%** |
| 有 `start_position` | **200** | **100%** |
| 无 `start_position` | **0** | **0%** |

### 2.3 对比结论

1. ✅ **新数据完全使用新 API 格式**：
   - 所有记录都有 `start_position` 字段
   - 所有记录都有 `exchange_side` 字段
   - Short 方向的 `signed_quantity` 全部为正数

2. ✅ **数据一致性显著提升**：
   - 之前：Short 方向 60% 负数，40% 正数（混合新旧 API）
   - 现在：Short 方向 100% 正数（全部新 API）

3. ✅ **符合新 API 规则**：
   - 新 API 中，`Size` 符号表示数量变化方向
   - Short 方向时，`Size` 为正数表示"增加/减少空头持仓"

## 3. 最新记录示例

```
ID: 458
Action: close_short
Side: short
SignedQuantity: 0.00011 (正数)
Quantity: 0.00011
RawDir: "Close Short"
StartPosition: -0.00557 (负数)
ExchangeSide: B
```

**观察**：
- ✅ `start_position` 为负数（做空持仓）
- ✅ `signed_quantity` 为正数（符合新 API 规则）
- ✅ `exchange_side` 为 "B"（平仓订单）

## 4. 结论

### 4.1 当前状态

1. ✅ **所有记录使用新版本 API**
2. ✅ **Short 方向的 `signed_quantity` 全部为正数**（100%）
3. ✅ **数据一致性完美**（无混合格式）
4. ✅ **所有记录都有 `start_position` 字段**

### 4.2 对配对策略的影响

由于新数据完全使用新 API 格式：
- ✅ **配对策略应使用 `Side` 字段判断方向**（已实现）
- ✅ **配对策略应使用绝对值进行数量匹配**（已实现）
- ✅ **不依赖 `signed_quantity` 符号**（已实现）

### 4.3 数据质量

- ✅ **数据完整性**：100% 记录有 `signed_quantity`
- ✅ **数据一致性**：100% 使用新 API 格式
- ✅ **符号规则统一**：Short 方向 100% 为正数

## 5. 建议

1. ✅ **保持当前配对策略**：使用 `Side` 字段和绝对值
2. ✅ **数据质量良好**：无需额外处理
3. ✅ **可以开始实现配对算法**：数据格式已统一

---

**文档版本**: 1.0  
**检查日期**: 2025-11-13  
**数据来源**: config.db.test (200 条记录，清理后重新同步)


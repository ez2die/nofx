# 分支测试指南

本文档说明如何测试 `nof1adapt-v2` 分支。

## 📋 测试方案概览

### 1. 快速测试（推荐）
运行自动化测试脚本，检查编译、代码质量等基础项。

### 2. 手动测试
按步骤手动验证各项功能。

### 3. 功能测试
启动服务并验证关键功能。

---

## 🚀 方法一：自动化测试脚本（推荐）

### 运行测试脚本

```bash
# 确保在 nof1adapt-v2 分支
git checkout nof1adapt-v2

# 运行测试脚本
./test_branch.sh
```

### 测试脚本会检查：

1. ✅ **当前分支** - 确认在 `nof1adapt-v2` 分支
2. ✅ **工作区状态** - 确认无未提交的更改
3. ✅ **代码格式** - 运行 `go fmt` 检查格式
4. ✅ **静态分析** - 运行 `go vet` 检查潜在问题
5. ✅ **编译测试** - 编译项目检查是否有编译错误
6. ✅ **依赖检查** - 验证 Go 模块依赖完整性
7. ✅ **配置文件** - 检查必要的配置文件是否存在
8. ✅ **关键文件** - 检查核心代码文件是否存在
9. ✅ **二进制文件** - 验证编译后的二进制文件格式
10. ✅ **服务启动** - 测试服务能否正常启动（可选）

---

## 🔧 方法二：手动测试步骤

### 步骤 1: 检查分支状态

```bash
# 确认当前分支
git branch --show-current
# 应该输出: nof1adapt-v2

# 检查工作区状态
git status
# 应该显示: working tree clean
```

### 步骤 2: 代码格式检查

```bash
# 格式化代码
go fmt ./...

# 检查是否有格式变更
git diff
# 应该没有输出（表示格式正确）
```

### 步骤 3: 静态分析

```bash
# 运行 go vet
go vet ./...
# 应该没有错误输出
```

### 步骤 4: 编译测试

```bash
# 编译项目
go build -o nofx_test .

# 检查编译是否成功
if [ -f "nofx_test" ]; then
    echo "✓ 编译成功"
    rm nofx_test
else
    echo "✗ 编译失败"
fi
```

### 步骤 5: 依赖检查

```bash
# 验证依赖
go mod verify

# 整理依赖（如果有问题）
go mod tidy
```

### 步骤 6: 检查关键文件

```bash
# 检查核心文件是否存在
ls -la main.go api/server.go manager/trader_manager.go decision/engine.go
```

---

## 🧪 方法三：功能测试（需要配置）

### 前提条件

1. ✅ 已配置 `config.json`（至少包含基本配置）
2. ✅ 端口 8080 未被占用
3. ✅ 可选：API 密钥（用于实际交易测试）

### 步骤 1: 准备配置文件

```bash
# 如果还没有配置文件，从示例创建
cp config.json.example config.json

# 编辑配置文件（至少设置基本参数）
nano config.json
```

### 步骤 2: 编译并启动服务

```bash
# 编译
go build -o nofx .

# 启动服务（前台运行，用于观察日志）
./nofx
```

### 步骤 3: 验证服务启动

**预期输出：**
```
╔════════════════════════════════════════════════════════════╗
║    🤖 AI多模型交易系统 - 支持 DeepSeek & Qwen                  ║
╚════════════════════════════════════════════════════════════╝

🌐 API服务器启动在 http://localhost:8080
```

### 步骤 4: 健康检查

在另一个终端运行：

```bash
# 测试健康检查端点
curl http://localhost:8080/api/health

# 预期输出：
# {"status":"ok","time":...}
```

### 步骤 5: 测试关键 API 端点

```bash
# 获取支持的模型
curl http://localhost:8080/api/supported-models

# 获取支持的交易所
curl http://localhost:8080/api/supported-exchanges

# 获取系统配置
curl http://localhost:8080/api/config
```

### 步骤 6: 停止服务

按 `Ctrl+C` 停止服务。

---

## 📊 测试结果评估

### ✅ 通过标准

- [x] 代码能够成功编译
- [x] `go vet` 无错误
- [x] `go fmt` 无格式问题
- [x] 服务能够正常启动
- [x] 健康检查端点可访问
- [x] 关键 API 端点响应正常

### ⚠️ 警告情况

如果出现以下情况，需要检查但可能不影响功能：

- 配置文件缺失（可以稍后配置）
- API 密钥未配置（不影响编译和启动）
- 端口被占用（切换到其他端口测试）

### ❌ 失败情况

如果出现以下情况，需要修复：

- 编译错误
- `go vet` 发现严重问题
- 服务无法启动
- 核心 API 端点无法访问

---

## 🔍 常见问题排查

### 问题 1: 编译失败

```bash
# 检查 Go 版本
go version
# 应该 >= 1.25.0

# 更新依赖
go mod tidy
go mod download
```

### 问题 2: 端口被占用

```bash
# 检查端口占用
lsof -i :8080

# 或者修改 config.json 中的端口
```

### 问题 3: 依赖问题

```bash
# 清理并重新下载依赖
go clean -modcache
go mod download
go mod verify
```

### 问题 4: 代码格式问题

```bash
# 自动修复格式
go fmt ./...

# 检查是否有变更
git diff
```

---

## 📝 测试报告模板

测试完成后，可以记录以下信息：

```markdown
## 测试报告

**分支**: nof1adapt-v2
**测试日期**: YYYY-MM-DD
**测试人员**: [你的名字]

### 测试结果

- [x] 编译测试: ✅ 通过
- [x] 代码格式: ✅ 通过
- [x] 静态分析: ✅ 通过
- [x] 服务启动: ✅ 通过
- [x] API 测试: ✅ 通过

### 发现问题

1. [如果有问题，记录在这里]

### 建议

1. [如果有建议，记录在这里]
```

---

## 🎯 下一步

测试通过后，可以：

1. **推送到远程仓库**
   ```bash
   git push origin nof1adapt-v2
   ```

2. **创建 Pull Request**（如果需要）

3. **部署到测试环境**（如果适用）

4. **继续开发**（如果测试通过）

---

**祝测试顺利！** 🚀

